import React from 'react';
import { Text, View, StyleSheet } from 'react-native';

const DisciplineScreen = () => {
    return (
        <View>
            <Text style={styles.text}>Discipline Screen</Text>
        </View>
    )
};

const styles = StyleSheet.create({
    text: {
        fontSize: 20
    }
})

export default DisciplineScreen;